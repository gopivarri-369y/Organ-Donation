<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Owner Dashboard</title>
    <style>
        .navbar{
            display: flex;           
        }
        .home{
            width: 10%;
            text-decoration: none;
            list-style: none;
            font-size: 40px;
            align-items: center;
            justify-content: center;
           
        }
        .home-button{
            /* background-color: red; */
            text-decoration: none;
            border: 1px;
            border-radius: 20%;
            border-color: black;
        }
        .title{
            width: 90%;
            /* background-color:blue; */
        }

        button{
            width: 100px;
            height: 30px;
            background-color: #4CAF50;
            border: 1rem;
            border-color: red;
            border-radius: 20px;
            margin-right: 45px;
        }
        .button{
            margin-left: 600px;
            margin-top: 40px;
        }
        table {
            width: 50%;
            border-collapse: collapse;
            margin-left: 450px;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
        }
        tr:nth-child(even) {
            background-color: #f9f9f9;
        }
        h1 {
            text-align: center;
        }
    </style>
</head>
<body>

    <div class="navbar">
        <div class="home">
            <a href="/home" class="home-button">Home</a>
        </div>
        <div class="title">
             <h1>Welcome to <%= owner.name %> Dashboard</h1>
        </div>
    </div>
   
    <table>
        <thead>
            <tr>
                <th>Detail</th>
                <th>Value</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td><strong>College</strong></td>
                <td><%= owner.collage %></td>
            </tr>
            <tr>
                <td><strong>Rooms For</strong></td>
                <td><%= owner.roomsFor %></td>
            </tr>
            <tr>
                <td><strong>Rent for 2-share</strong></td>
                <td>$<%= owner.rent2Share %></td>
            </tr>
            <tr>
                <td><strong>Rent for 3-share</strong></td>
                <td>$<%= owner.rent3Share %></td>
            </tr>
            <tr>
                <td><strong>Rent for 4-share</strong></td>
                <td>$<%= owner.rent4Share %></td>
            </tr>
        </tbody>
    </table>
    <div class="button">
        <!-- Update Form -->
        <form action="/login-owner/update" method="POST" style="display:inline;">
            <!-- Hidden input to pass owner ID or other data -->
            <input type="hidden" name="ownerId" value="<%= owner.id %>">
            <button type="submit">Update</button>
        </form>

        <!-- Delete Form -->
        <form action="/login-owner/delete" method="POST" style="display:inline;">
            <!-- Hidden input to pass owner ID or other data -->
            <input type="hidden" name="ownerId" value="<%= owner.id %>">
            <button type="submit">Delete</button>
        </form>
    </div>
    
</body>
</html>
